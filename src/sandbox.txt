    public void addTag(int[] posUsr[], String tag) {
        for (int i=0; i < posUsr; i++) {
            if (!(posReal <= 99 && posReal >= 0) || contatos[posReal] == null) {
                throw new IllegalArgumentException("POSIÇÃO INVÁLIDA");
            } else {
                contatos[posReal].addTag(tag);
            }
        }

    }



     public void addTag(String tag) throws NullPointerException, IllegalArgumentException {
            if (tag == null) {
                throw new NullPointerException("TELEFONE INVÁLIDO");
            }
            if (tag.isBlank()) {
                throw new IllegalArgumentException("TELEFONE INVÁLIDO");
            }
